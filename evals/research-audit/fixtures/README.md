# Research audit fixtures

number-fidelity-cases.json is a deterministic, network-free fixture for the
unit and percent scale bindings used by the report gate. Full report fixtures
are assembled with research-core identity helpers in the unit test, keeping
every reference a valid content ID.
