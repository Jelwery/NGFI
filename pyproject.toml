[project]
name = "finance2dsh-yfinance-runner"
version = "0.1.0"
description = "yfinance process adapter for NGFI"
requires-python = ">=3.12"
dependencies = [
  "yfinance==1.5.2",
]

[dependency-groups]
dev = [
    "pyyaml>=6.0.3",
]

