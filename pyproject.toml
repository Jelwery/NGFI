[project]
name = "finance2dsh-yfinance-runner"
version = "0.1.0"
description = "yfinance process adapter for NGFI"
requires-python = ">=3.12"
dependencies = [
  "baostock>=0.8",
  "mootdx>=0.10",
  "numpy>=2.0",
  "openpyxl>=3.1",
  "pandas>=2.2",
  "requests>=2.32",
  "xlrd>=2.0",
  "yfinance==1.5.2",
]

[dependency-groups]
dev = [
    "pyyaml>=6.0.3",
]

